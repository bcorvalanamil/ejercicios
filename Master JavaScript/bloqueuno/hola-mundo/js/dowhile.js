'use strict'

// do while primero ejecutamos un bloque de codigo y luego la condicion, que se puede ejecutar una vaz 

// Nos permite ejecutar un bloque de instruciones y luego evaluar la condicion 

/*
var ano = 20;

do{ alert ("SOLO CUANDO SEA DIFRENTE A 20 ");
ano = 20;


} while(ano !=20)

*/


// Si ano es difrente a 20 se ejecutaria dos veces. Pero como en este caso es igual a 20 solo se va a ejecutar una vez.

/*
var anodos = 20;

do{ alert ("funciona");
anodos++;

} while(anodos >=25)

*/

var anodos = 30;

do{ alert ("funciona");
anodos--;

} while(anodos > 25)

ano= 20;

// recordar que con break; se corta el bucle